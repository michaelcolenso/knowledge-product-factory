Create the complete, buyer-ready content for this support artifact. Write it as if it will be delivered directly to the buyer with no further editing.

Requirements:
- Make it immediately usable - no placeholders except intentional [BUYER FILLS IN] fields
- Include all necessary context within the artifact itself (don't require reading the main product to use it)
- For checklists: Use checkboxes [ ] and number sequential items
- For templates: Include instructions in italics or brackets
- For spreadsheets (CSV): Include a header row, example rows, and clear column descriptions in a comment row
- For scripts: Include caller/recipient roles, pause instructions, and objection responses

Length: As long as it needs to be to be genuinely useful. Don't pad, don't truncate.

Output the artifact content directly. No JSON. No schema. Just the artifact.
