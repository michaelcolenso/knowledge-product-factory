class AnthropicAdapter:
    def generate(self, system_prompt: str, user_prompt: str, temperature: float = 0.2) -> str:
        raise NotImplementedError("AnthropicAdapter placeholder. Configure provider integration.")
